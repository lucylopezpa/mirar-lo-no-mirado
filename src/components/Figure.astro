---
const { src, caption } = Astro.props;
---

<figure class="relative right-1/2 left-1/2 w-screen -ml-[50vw] -mr-[50vw]">
  <img src={src} alt="" />
  {
    caption && (
      <figcaption class="mt-4 px-8 font-antarctican-mono italic text-xs">
        {caption}
      </figcaption>
    )
  }
</figure>

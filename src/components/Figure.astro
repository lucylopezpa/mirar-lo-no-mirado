---
const { src, caption } = Astro.props;
---

<figure>
  <img src={src} class="w-full h-auto" alt="" />
  {
    caption && (
      <figcaption class="mt-4 px-6 font-antarctican-mono italic text-xs">
        {caption}
      </figcaption>
    )
  }
</figure>
